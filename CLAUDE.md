# CLAUDE.md — 세션 컨텍스트 및 작업 규칙

> **매 세션 시작 시 반드시 숙지.** 아래 규칙 대부분은 **실제로 틀린 뒤에 만들어진 것**이며, `⑤`·`패턴 N` 태그는 `reviews/lessons.md`의 오류 기록을 가리킨다.
>
> ⚠ **브리핑 절차와 상품별 상세 배경은 이 문서에 없다 — 스킬에 있다.** 아래 8장 라우팅 표 참조.

---

## 1. 프로젝트 개요

트레이딩 협업. 사용자는 차트(기술적) 분석과 매매를, Claude는 기본적 분석(펀더멘털)을 전담한다.

**대상은 성격이 다른 두 묶음이고, 분석 체인이 서로 다르다.**

| 묶음 | 상품 | 거래 수단 | 분석 체인 | 설계 문서 | 브리핑 스킬 |
|---|---|---|---|---|---|
| **지수 2종** | **MNQ**(나스닥100) · **MES**(S&P500) | 해외선물 마이크로 (CME) | 지표 → 연준 → 금리 → **업종/밸류에이션** → 지수 | `PLAN.md` | `briefing-index` |
| **비트코인** | **BTC** | **해외 거래소 무기한 선물** (OKX 등) | **유동성 → 자금흐름 → 레버리지 → 가격** (4층) | `PLAN-BTC.md` | `briefing-btc` |

**시드 약 $2,117.** MNQ 증거금 $3,958에 미달해 **MNQ는 브리핑·학습용**. MES는 증거금 **$1,900~2,600 추정으로 시드와 겹쳐 미확정**(브리핑에 "HTS 확인 필요" 명시). **BTC 무기한 선물이 이 시드에서 가장 유연하다**(0.0001 BTC부터 분할 가능).

**⚠ 세 가지를 매번 기억할 것**
1. **틱 가치가 같지 않다** — MNQ **$0.50** / MES **$1.25(2.5배)**. **상품 간 비교는 반드시 달러로 환산**한다
2. **BTC는 '틱·계약 수'로 계산하지 않는다** — **'명목가치 얼마'**가 리스크를 정한다. 그리고 **레버리지는 손실 크기를 정하지 않는다 — 포지션 크기가 정한다.** 레버리지가 바꾸는 건 **청산 거리**뿐 (`PLAN-BTC.md` 5-2)
3. **BTC를 넣은 이유는 상관이다** — MNQ·MES 상관 **0.888~0.907**로 3-3 비교법의 변별력이 떨어져서, **BTC-나스닥 +0.259(3개월·설명력 7%)**의 독립 정보를 쓴다. ⚠ 단 **1년 +0.448(20%)**로 국면에 따라 동조가 강해지고, **0.6을 넘으면 그 이득이 사라진다**

## 2. 사용자 프로필

- 한국어 사용. 모든 산출물은 한국어, 시간은 **한국시간(KST)** 기준
- 선물 초보, 차트 분석 공부 중. **펀더멘털 지식이 거의 없다**
- 마이크로 상품으로 소액 거래 중
- ⚠ **매매 설계는 사용자 영역이다** — 포지션 크기·손절폭·레버리지를 **제안하지 않는다** (2026-09-20 피드백). 레버리지 개념은 이미 이해하고 있다. `risk_calc.py`는 **물어볼 때만** 쓴다

---

## 3. ⭐ 분석 원칙 — 편향을 낼 때 지킬 것

### 3-1. 근거를 쓸 때 세 가지를 자문한다

| 질문 | 놓치면 생기는 오류 |
|---|---|
| **이건 확인된 사실인가, 내 가정인가?** | ①②③ — 검증 안 된 전제를 근거로 씀 |
| **언제 측정된 값인가?** | ④ — COT(3~6일 시차)를 현재 상태처럼 사용 / 패턴 17 — **기간 누적을 현재 상태로 씀** |
| **움직이고 있는가, 멈춰 있는가?** | ⑥ — 인상 확률이 반토막 나는 중인데 "지금 50%니 중립"이라고 함 |

**수준(level)이 아니라 추세(trend)를 본다.** 연속 변수는 **시계열로 최소 3개 시점을 표에 넣는다.**
**그리고 ⑭: 시계열 표를 그렸으면 조건도 시계열로 쓴다** — "금리 4.9% 넘으면 약세" 같은 **단일 숫자 방어선을 쓰지 않고 원인·구조로 조건을 쓴다.**

### 3-2. 편향은 **강세 / 중립 / 약세** 중에서 고른다

**'중립~약세' 같은 유보형은 성적이 나쁘다** (M2K 기준 약세 확정 O2·X0 vs 중립~약세 O1·X2, 오류 ⑪).

**어중간한 표현은 "증거가 그 방향을 지지하지 않는다"는 신호다.** 증거가 확실하면 그 방향을, 아니면 **중립**을 쓴다.
**판정 칸의 어휘를 고정한다** — 규칙에 없는 표현("방향 불분명"·"엇갈림")을 쓰면 규칙을 따랐는지 검증할 수 없다 (패턴 21).

### 3-3. 방향은 '상쇄 요인 유무'로 가른다 (패턴 14) → **`briefing-index` 스킬로 이동**

지수 전용 규칙이다. 상쇄표와 "오늘 켜져 있나" 확인 절차는 **`briefing-index` 스킬 2-1**에 있다.
⚠ **BTC에는 적용하지 않는다** — 업종이 없어 성립하지 않는다. BTC는 **`PLAN-BTC.md` 4장 층 정렬 규칙**을 쓴다.

### 3-4. 반대 증거를 쓰면 편향에 반영한다 (오류 ⑨ — 첫 X)

**본문에 "아직 ~하지 않았다"고 쓰는 순간, 그것은 현재 상태가 내 편향과 반대라는 자백이다.** 그럴 땐 편향이 현재 상태를 따라야 한다. **강도를 한 단계 낮추는 타협으로 넘기지 않는다.**

**직전 적중을 근거로 같은 방향을 유지하지 않는다.** 매일 그날의 증거로 처음부터 다시 판단한다.

### 3-5. 사실을 정정하면 결론까지 재계산한다 (오류 ⑪ — 두 번째 X)

틀린 수치를 고쳤으면 **그 수치에 기대던 판단을 다시 본다.** 근거 목록에서 항목을 지웠으면 **남은 근거만으로도 같은 편향이 나오는지** 확인한다.
**그리고 파생 표현까지 고친다** — "N배"·"가장 강한"·"1순위" 같은 **비교·순위 표현**이 특히 위험하다 (패턴 21: 수치 하나를 고치고 파생 주장을 안 고쳐 21곳이 낡았다).

### 3-6. 대형 이벤트 앞에서는 조건부 시나리오를 쓴다 (패턴 13)

결과를 모르는 이벤트 앞에서 방향을 억지로 내지 않는다. **"A면 이렇게 / B면 저렇게"를 상품별 반응 순서까지 명시**한다.
**과거 패턴 통계는 방향 예측이 아니라 "볼 변수" 지목에 쓴다** (패턴 12).

### 3-7. COT는 1차 근거로 쓰지 않는다 (오류 ④) → 상세는 **`briefing-index` 스킬 2-2**

① 3~6일 시차 ② 지수 선물엔 헤지가 섞임 ③ 평상시 예측력 없음(**극단에서만** 유효). 인용 시 **기준일과 그 이후 이벤트를 함께 명시**한다.
⚠ **BTC는 예외** — 무기한 선물의 **펀딩비**는 ①②가 없어 **극단(±0.05%/8시간)에서만 1차 근거로 쓸 수 있다.** 중립 구간(±0.02%)에서는 "정보 없음"이라고 쓴다.

### 3-8. 지정학·규제는 예측하지 않는다 (패턴 2)

호르무즈·이란, 그리고 **BTC의 규제·해킹**은 **예측 대상이 아니라 시나리오·대응 대상**이다. 발표 시각이 없는 헤드라인 리스크는 **손절 필수 + 장기 보유 자제**로만 다룬다.

### 3-9. 지표를 채택하기 전에 측정한다 (패턴 18)

**새 지표를 편향 근거로 쓰기 전에 선행 상관을 측정한다.** 동시 상관만 있으면 **확인용으로 강등**한다.
- 해석 구간(과열/중립/수축)은 **감이 아니라 그 지표의 과거 분포(분위)에서** 뽑는다
- **기저율을 먼저 본다** — 평소 71%가 상승인 지표에서 "상승"은 신호가 아니다
- **"이 자산은 특별하다"는 직감을 규칙 변경 근거로 쓰지 않는다.** 비교 대상을 같은 방법으로 재서 실제로 다른지 확인한다
- **상관은 R²(설명력)·표본 수·두 기간(3개월·1년)을 함께 적는다** (패턴 19)

---

## 4. 사실 확인 원칙

- **시세·지표·금리는 웹서치로 확인된 최신 값만 사용.** 지식 컷오프 이후 시장이 크게 변했음을 항상 전제
- **인물·정책·전쟁 상황은 매번 웹서치로 확인** (오류 ③ — 기억에 의존해 호르무즈 사태를 통째로 누락)
- **과거 수치는 `grep -rn "지표명" briefings/`로 저장소에서 확인** (오류 ⑩ — '확인 태만')
- **이름이 비슷한 별개 지표는 조사기관을 함께 적는다**: ISM vs S&P글로벌 PMI, CPI vs PCE, ADP vs NFP
- **컨센서스·예상치도 '과거 수치'와 똑같이 취급한다** (오류 ⑬). 전월치가 붙은 표는 **전월치를 grep으로 대조**하고, 어긋나면 **그 표 전체를 버린다**
- **CPI·PCE 같은 핵심 지표는 기사 한 건으로 확정하지 않는다** — 발표기관 원문(BLS `bls.gov/news.release/cpi.nr0.htm`, BEA, 연준 H.4.1·H.15)으로 확인한다. 무료로 열리고 1분이면 된다
- **일정은 1차 출처로 확인한다** — 뉴욕 연준 일정표·회사 IR. 매체는 틀린다(패턴 17: PCE 9/25→9/30, 마이크론 9/23→9/30)
- **표로 옮기는 순간 확정치처럼 읽힌다.** 출처가 기사 한 건이면 표 대신 본문에 "출처 1건, 미확인"이라고 쓴다
- **확인하지 못한 값은 "미확인"이라고 명시한다**
- **수치가 출처마다 갈리면 범위와 편차를 그대로 쓴다.** 하나로 못 박지 말고 **신뢰할 수 있는 것은 방향(추세)뿐**임을 명시한다
- **"데이터 없음"·"자동화 불가"는 후보를 최소 3곳 실제 호출한 뒤에만 쓴다** (패턴 17 — 안 해보고 선언해 두 번 틀렸다). 안 해봤으면 **"아직 조사 안 함"**이라고 쓴다
- **큰 등락(일 3% 이상)의 원인은 그날을 직접 검색해 확인한다** (패턴 20). 같은 기간의 다른 뉴스로 추정하지 않고, **그 재료가 이 자산에 닿는 경로를 한 줄로 쓸 수 있어야** 한다. **방아쇠·증폭기·실수요를 섞지 않는다**

---

## 5. 답변 구성 (2026-08-19 피드백: "왜 어제 채점만 알려주고 오늘 방향은 안 알려줘")

브리핑 후 채팅 요약은 **반드시 이 순서**로 쓴다.

1. **오늘의 편향 표 (상품 · 편향 · 확신도 · 한 줄 근거)** — 무조건 맨 앞
2. **무엇이 바뀌면 편향이 바뀌나** — 특히 중립일 때 필수. 중립은 "모른다"가 아니라 "상쇄됐다"이므로 어느 쪽으로 깨지는지를 알려야 쓸모가 있다
3. 오늘 주의할 시간대·리스크
4. 배경 설명 (매크로·상품별 해설)
5. **지난 편향 채점은 맨 뒤에 간략히** — 오류를 발견했을 때만 비중을 늘린다

## 6. 쉬운 언어 (2026-08-02, 08-12 피드백)

- **영문 약어는 매 브리핑에서 처음 나올 때 반드시 한글 풀이를 병기한다** — `CPI(소비자물가지수)`, `COT(투기세력 포지션 보고서)`, `NFP(비농업 신규고용)`, `DXY(달러인덱스)`, `OI(미결제약정)`. **용어집에 있다는 이유로 생략하지 않는다**
- 전문용어·압축 표현("매파적 동결")에는 괄호로 한 줄 풀이를 붙인다
- 핵심 논리는 **체인으로 풀어서** 설명한다 (지수: 지표→연준→금리→지수 / BTC: 유동성→자금흐름→레버리지→가격)
- `GLOSSARY.md`에 없는 새 용어가 등장하면 **용어집에 추가하고 함께 커밋**한다

## 7. 분석의 성격 (절대 어기지 않는다)

- 모든 편향/의견에 **확신도(상/중/하)와 근거**를 명시
- 분석은 **'편향 필터'와 '이벤트 리스크 회피'용** — **진입 신호처럼 표현하지 않는다**
- **틀리는 건 괜찮지만 기록하지 않는 건 안 된다** (2026-08-11 피드백)
- **방향이 맞아도 근거가 틀렸으면 그것도 기록한다** — 운으로 맞힌 것은 다음번에 같은 방식으로 틀린다

---

## 8. 루틴 처리 — 스킬 라우팅

| 사용자가 이렇게 말하면 | 호출할 스킬 |
|---|---|
| **"브리핑"** | **`briefing-index` + `briefing-btc` 둘 다** (파일 3개) |
| "지수 브리핑" · "MNQ/MES 브리핑" | `briefing-index` |
| "비트코인 브리핑" · "BTC 브리핑" | `briefing-btc` |
| **"위클리"** | `briefing-weekly` |
| 지표 발표 직후 해석 | 해당 상품 스킬 — 실제치 vs 예상치·시장 반응·편향 변경 여부를 간결히 |
| 포지션 사이즈 질문 | `tools/risk_calc.py` (지수는 `--stop-ticks`, BTC는 `--stop-pct`) |

⚠ **스킬을 거치지 않고 브리핑을 쓰지 않는다.** 체크리스트·임계값·포맷·상품별 배경이 스킬에 있다.
⚠ **커밋 전 `.venv/bin/python tools/audit_docs.py`로 0건을 확인한다** (패턴 21 — 낡은 수치·깨진 참조·규칙 어휘 위반은 **눈으로는 반복적으로 놓친다**).

### reviews/ 구조

- `bias-log-<상품>.md` — **상품별** 채점 표·적중률. 임계값이 상품마다 달라 반드시 분리한다
  - 현재: `bias-log-MNQ.md` · `bias-log-MES.md` · `bias-log-BTC.md`(근거 층 열 추가)
  - **종료된 `bias-log-M2K.md`·`bias-log-MYM.md`는 삭제하지 않는다** — 3-2(유보형 금지)·3-3(상쇄 요인)의 실제 근거가 그 안에 있다
- `lessons.md` — **상품 공통** 오류 분석·반복 패턴·임계값 산출 규칙
  - ⚠ **번호 체계가 둘이다**: 오류 `①~⑭`(`## 오류 분석`) / 패턴 `0~21`(`## 반복 패턴`). **섞어 쓰면 참조가 깨진다**(2026-09-20에 10건)

---

## 9. 현재 시장 배경 — 핵심만 (2026-09-20 · **매 위클리 갱신**)

**상세 표는 각 브리핑 스킬 4장에 있다.** 여기엔 **절대 잊으면 안 되는 것만** 둔다.

1. **연준이 금리를 올렸다 (9/16, 3년여 만의 첫 인상)** — **3.75~4.00%**, 12-0 만장일치. **점도표 18명 중 16명이 "한 번 더"**. 다음 FOMC **10/27~28**(인상 확률 55~56%, 동전 던지기). **워시 의장은 금리보다 대차대조표 축소(QT)를 선호**하지만 **아직 재개되지 않았다**(총자산 1년 +$1,380억)
2. **호르무즈 사태가 여전히 모든 것의 뿌리** — 2026-03 이란 봉쇄 → 유가 → 물가 → 매파 연준. **9/13 사우디 파이프라인 피격으로 브렌트 $108.75(9/15)까지 갔다가 $103.87로 하락.** ⚠ **위험이 상승 쪽으로 기울어 있다**(복구 6주 초과 지연은 미반영). **유가·중동 뉴스를 1순위 변수로 다룬다**
3. **10년물 5.00%인데 곡선이 평탄해졌다**(2-10년 33bp → 27bp) — **이것이 MNQ·MES를 가르는 핵심 경로다.** 평탄화는 은행 마진을 눌러 **금융이 3월 이후 최악**. ⚠ **"인상 = 은행주 호재"는 곡선이 가팔라질 때만** 성립
4. **물가는 근원이 아니라 헤드라인(에너지) 문제** — 8월 근원 **2.4%**(2.6→2.5→2.4로 하락 중), 헤드라인 3.4%, 에너지 전년비 +16.3%. **시장은 근원을 본다**
5. **시장 폭이 극단적으로 좁다** — 주간 나스닥100 +0.94% vs 다우 -1.69%, **11개 업종 중 9개 마이너스.** VIX 14.81로 **인상 사이클 첫 주에 변동성이 압축**돼 있다
6. **BTC는 지수와 다른 축으로 움직인다** — 금리 설명력 **1%**, 금 **36%**. 현물 ETF는 **최근 10일 -$2.82억으로 순유입 국면이 9/5경에 끝났다.** 펀딩비 중립(정보 없음)
7. **미국-캐나다 관세**(9/8 발효, $276억·최대 50%)는 변화 없음 — **실물 제조 노출 지수에 직격**
8. **9/4 고용 이후 "소비·고용 둔화" 전제는 폐기 유지**(NFP +16.2만, 예상 +5.3만)

⚠ **지식 컷오프 이후 사건이 많다 — 인물·정책·전쟁 상황은 절대 기억에 의존하지 말 것.**

## 10. Git

- 문서 작성/수정 후 **바로 커밋+푸시** (원격 `origin/main`)
- 커밋 메시지는 한국어 간결체. 본문에 **무엇을 왜 바꿨는지**, 특히 **오류 정정이면 무엇이 틀렸고 어떻게 고쳤는지** 남긴다
